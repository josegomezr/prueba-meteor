import { Meteor } from 'meteor/meteor';
// importar las apis, sin esto no funciona el registro.
import '../imports/api/contactos.js';

Meteor.startup(() => {
  // code to run on server at startup
});
